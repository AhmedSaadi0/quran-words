# خطة التحقق الشامل لكل الجذور (1,642) وتصحيح الملخصات الخاطئة

> الهدف: مراجعة **كل الجذور (1,642)** — ليس فقط التي تبدأ بـ `ى` أو بلا معانٍ — للتأكد أن (1) الجذر المخزن هو فعلاً جذر الكلمات في الآيات، و(2) الملخص المولد بالذكاء الاصطناعي (`root_ai_summary`) مطابق للمعنى الحقيقي في المعاجم، وتصحيح الحالات الخاطئة مثل `ىبب`.

**المثال الكاشف:**
- الآية: `وَفَـٰكِهَةًۭ وَأَبًّۭا` (عبس:31)
- الجذر المخزن: `ىبب` (id ?) — الملخص الحالي: *«يَدُلّ على الخلاءِ والخرابِ… أرضٌ يَبابٌ»* — هذا **يَباب** (الصحراء الخالية)
- المعنى الصحيح: **`أَبّ`** — الكلأ والمرعى الذي تأكله الأنعام (الراغب، لسان العرب: `الأَبُّ: التبنُ والكَلأُ` ) — خطأ فادح سببه تطبيع الهمزة `أ`→`ى` وخلط `أ-ب-ب` مع `ي-ب-ب`

الخطأ يثبت أن حتى الجذور التي لها معانٍ (1,580) قد تحمل ملخصات خاطئة إذا كان الجذر نفسه محرفاً.

---

## 📊 الوضع الحالي بعد إنجاز الخطط السابقة

| الملف | الحالة |
|-------|--------|
| `done/ROOT_AI_SUMMARY_PLAN.md` | خطة إنشاء الملخصات (1,580) — **مكتملة** (78 `ox-alpha` + 1,502 `muse-spark-1.2-contributor-free`) |
| `done/ROOTS_62_VERIFICATION_PLAN.md` | خطة التحقق من 87 جذراً (68 تبدأ بـ ى + 19) — **مكتملة** (67 صُححت، 43 وُلدت لها ملخصات، بقي 19 بلا مادة) |
| `done/IMPLEMENTATION_PLAN.md` | خطة البناء العامة — **مكتملة** |
| `data/root_ai_summary.json` | 1,642 مفتاح (1,623 مُنجز + 19 `null` بلا مادة) |
| `quran_words.db:root_ai_summary` | 1,623 صفاً |
| `data/verify_roots/` | 87 حزمة + `index.json/csv` + `report.md` للـ68 ى |
| `data/roots_without_meanings.json` | 19 جذراً بلا مادة (بعد التصحيح) |

**المشكلة المتبقية:** لا يوجد تحقق للـ1,555 جذراً الأخرى التي لم تُفحص (1,642 - 87). قد تحمل أخطاء مشابهة لـ `ىبب`→`أبّ` لكنها مرت لأن لها معانٍ فلم تُشكّ.

---

## 🎯 نطاق الخطة الجديدة — كل الجذور

- **النطاق:** 1,642 جذراً كاملاً
- **الأولوية 1:** الـ1,623 التي لها ملخص — فحص هل الملخص مطابق للمعنى المعجمي أم لا (مثل `ىبب`/`أبّ`)
- **الأولوية 2:** الـ19 بلا مادة — محاولة أخيرة لجلب مادة أو تلخيص من السياق القرآني
- **الأولوية 3:** كل الجذور التي تبدأ بـ `ى` (68) أُعيد فحصها ضمناً لكن بمعيار أدق (هل الجذر هو فعلاً جذر الكلمات في الآيات؟)

---

## 🔍 التشخيص — أنواع الأخطاء المتوقعة

1. **تطبيع الهمزة الخاطئ:** `ى` أولاً بدل `أ/ء` → `ىبب` (أبّ/يَباب)، `ىمن` (أمن)، `ىكل` (أكل) — الملخص يُبنى على مادة خاطئة.
2. **خلط الجذر الثنائي/المضعف:** `دمو`→`دم`، `زلزل` مضعف رباعي — الملخص قد يخلط.
3. **هلوسة AI رغم صحة الجذر:** حتى مع جذر صحيح، الملخص قد يضيف معاني غير موجودة في `root_meanings` (كما في `ىبب` حيث استُخدمت مادة `يبب` بدل `أبب`).
4. **التباس اللمّة:** `بوى` (بوأ) لمّته `بَىْءَ` لكن الجذر المخزن `بوى` — قد يُلخص خطأ.

---

## 🗺️ مراحل التنفيذ

### المرحلة 0 — تصدير الأدلة لكل الجذور (يوم 1)

**سكريبت:** `scripts/verify_all_export.py` (توسعة لـ `verify_roots_export.py`)

- لكل من 1,642: اجلب
  ```sql
  roots r → word_morphology wm → word_ayah wa → words w → ayat a
  LEFT JOIN lemmas l
  LEFT JOIN root_meanings rm (count)
  LEFT JOIN root_ai_summary ai
  ```
- اكتب `data/verify_all/{root}.json`:
  ```json
  {
    "root_stored": "ىبب",
    "root_id": 1589,
    "quran_occurrences": 1,
    "distinct_words": ["وَأَبًّۭا"],
    "distinct_lemmas": ["وَأَبًّۭا"],
    "ayat_sample": [{"surah":80,"ayah":31,"word":"وَأَبًّۭا","text":"وَفَاكِهَةً وَأَبًّا"}],
    "meanings_count": 4,
    "meanings_sample": [{"book":"لسان العرب","definition":"الأَبُّ: الكلأ..."}],
    "ai_summary_ar": "يَدُلّ على الخلاءِ والخرابِ...",
    "ai_model": "ox-alpha",
    "needs_review": true
  }
  ```
- الخرج: `data/verify_all/` (1,642 ملفاً) + `data/verify_all/index.json`

### المرحلة 1 — فحص صحة الجذر + صحة الملخص (يوم 1–3)

**سكريبت:** `scripts/verify_all_check.py`

1. **صحة الجذر:** كما في الخطة السابقة — هل `root_stored` يطابق `root` في `quranic_corpus_morphology.json` واللمّة؟ هل `ى` أولاً يجب أن تكون `أ/ء`؟ (يُستخدم `data/verify_roots/` كمرجع للـ87)
2. **صحة الملخص:** مقارنة آلية + مراجعة عينات يدوية:
   - **آلياً:** هل كلمات الملخص تطابق `root_meanings.definition`؟ استخدم `rapidfuzz` أو تضمين لغوي (embedding) لقياس التشابه — إذا كان التشابه <0.60 يُوسم `suspect`.
   - **يدوياً:** عينة 100 جذر عشوائي + كل الجذور التي ذُكرت كأمثلة خاطئة (`ىبب`/`أبّ`، `ىله`/`أله`…) — مراجعة بشرية عبر `data/verify_all/report.md`.

- التصنيف الجديد:
  ```
  root_id,root_stored,root_corrected,summary_status,root_status,confidence,example_ayah,note
  1589,ىبب,أبب,ملخص خاطئ (يَباب بدل أَبّ),مُصحح,0.95,"80:31 وَأَبًّا","المادة الصحيحة هي أبّ: الكلأ"
  22,ىمن,أمن,ملخص صحيح لكن الجذر محرف,مُصحح محتمل,0.80,"2:3 يُؤْمِنُونَ","له 7 معاجم تحت ىمن لكن الأصل أمن"
  ```

- الخرج: `data/verify_all/verification.csv` + `data/verify_all/report.md`

### المرحلة 2 — تصحيح الجذور وإعادة جلب المعاني (يوم 3–4)

**سكريبت:** `scripts/verify_all_fix.py` (توسعة لـ `fetch_missing_meanings.py`)

- لكل صف `root_status=مُصحح` بثقة ≥0.90: حدّث `roots.root` و `root_meanings` كما في الخطة السابقة (من `arabic_roots.json` بالمفتاح المصحح).
- للجذور التي بقيت `مُصحح محتمل` بثقة أقل: لا يُحدَّث `roots` بل يُضاف حقل `root_corrected` للمراجعة.

### المرحلة 3 — إعادة توليد الملخصات الخاطئة فقط (يوم 4–6)

- **لا إعادة لكل الجذور** — فقط التي وُسمت `summary_status=ملخص خاطئ` أو `root_status=مُصحح`.
- أعد تصدير الحزم: `python scripts/export_root_bundles.py --roots "أبب,أمن,أله,..."`
- احذف ملخصاتها القديمة من `data/root_ai_summary.json` (أعدها `null`) ثم أعد الطابور:
  ```bash
  python scripts/build_summary_queue.py  # سيجد N ملخصاً خاطئاً أصبح null
  ```
- ولّد من جديد بوكلاء `muse-spark-1.2-contributor-free` (يكتبه الوكيل بنفسه، 3–6 جمل بتشكيل وافٍ، انتظار 2–3 ثوانٍ) — كما في `scripts/merge_summaries.py:22`.

### المرحلة 4 — التحقق النهائي والتوثيق (يوم 6)

- `python scripts/build_root_ai_summary.py --stats` → 1,623 مُنجز (أو أكثر إذا أُضيفت الـ19)
- اختبار `RootSerializer` على `ىبب/أبب` → يجب أن يعود `الأَبُّ: الكلأ...` لا `يَباب`.
- تحديث `README.md` + نقل هذه الخطة إلى `done/` بعد الإنجاز.

---

## 📁 المخرجات المتوقعة

| الملف | الوصف |
|-------|--------|
| `data/verify_all/*.json` (1,642) | أدلة لكل جذر (آيات + معانٍ + ملخص حالي) |
| `data/verify_all/verification.csv` | جدول التصنيف لكل الجذور |
| `data/verify_all/report.md` | تقرير الأخطاء (مثل `ىبب`→`أبّ`) |
| `quran_words.db:roots` | جذور مصححة (ثقة عالية فقط) |
| `quran_words.db:root_meanings` | معانٍ مضافة للمصحح |
| `quran_words.db:root_ai_summary` | ملخصات مصححة (N صفاً مُستبدلاً) |
| `data/root_ai_summary.json` | مُحدث (الملخصات الخاطئة مُستبدلة) |

---

## ⚠️ ضوابط

- **لا حذف للمُنجز الصحيح:** يُستبدل فقط ما وُسم `ملخص خاطئ`/`مُصحح` (إلا بـ `--replace`).
- **تصحيح الجذر بثقة عالية فقط** — وإلا يُحفظ كـ `root_corrected` دون تعديل `roots.root`.
- **التحقق من الملخص آلياً + يدوياً** — التشابه النصي وحده لا يكفي؛ عينة 100 تُراجع يدوياً.
- **الـ19 بلا مادة** تبقى `null` إلا إذا وُجدت مادة جديدة — لا هلوسة من السياق إلا بوسم واضح.

---

## ▶️ أمر البدء السريع (بعد اعتماد الخطة)

```bash
python scripts/verify_all_export.py            # المرحلة 0 — 1,642 حزمة
python scripts/verify_all_check.py --review    # المرحلة 1 — يبني CSV والتقرير
# بعد المراجعة اليدوية لعينة ىبب/أبّ:
python scripts/verify_all_fix.py --apply       # المرحلة 2 — يصحح الجذور ويجلب المعاني
python scripts/export_root_bundles.py --roots "أبب,أمن"  # أو كل المصحح
python scripts/build_summary_queue.py
# موجات الوكلاء (3–4 دفعات متوازية، 2–3 ثوانٍ فاصل) كما سبق
python scripts/merge_summaries.py
/usr/bin/python3.13 scripts/build_root_ai_summary.py
```
